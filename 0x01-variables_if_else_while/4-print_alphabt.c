#include <stdio.h>
/**
 * main - Entry point
 * Return: 0
 */
int main(void)
{
	char c;

	c = 'a';
	while
		(c <= '2') {
			if ((c != 'q' && c != 'e') && c <= '2')
				putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
